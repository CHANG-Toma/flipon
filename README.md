# FlipOn

MVP web — application couple anti-routine.

## Lancer

```bash
cd flipon
npm install
npm run dev
```

Ouvrir [http://localhost:3000](http://localhost:3000).

## Pages

- `/` — Landing + waitlist
- `/presentation` — Présentation produit
- `/test` — Démo interactive (contraintes → swipe → match)
