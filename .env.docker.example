# Copier vers .env.docker puis changer les mots de passe
# cp .env.docker.example .env.docker

POSTGRES_USER=flipon
POSTGRES_PASSWORD=change-me-local-pg-only
POSTGRES_DB=flipon
REDIS_PASSWORD=change-me-local-redis-only
